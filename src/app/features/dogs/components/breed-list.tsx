import { getDogBreeds } from "@/app/features/dogs/lib/actions";
import React from "react";

const BreedList: React.FC = async () => {
  // const breeds = await getDogBreeds();

  // console.log("breeds: ", breeds);
  return <>Hello</>;
};

export default BreedList;
